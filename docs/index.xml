<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Residual Thoughts</title>
<link>https://aikkala.github.io/residual-thoughts/</link>
<atom:link href="https://aikkala.github.io/residual-thoughts/index.xml" rel="self" type="application/rss+xml"/>
<description>This blog presents research ideas I&#39;ve explored, but which have not (at least for the time being) been made into papers</description>
<generator>quarto-1.9.37</generator>
<lastBuildDate>Sun, 24 May 2026 21:00:00 GMT</lastBuildDate>
<item>
  <title>Do LLMs Know What They Don’t Know? Teaching Confidence with RL</title>
  <dc:creator>Aleksi Ikkala</dc:creator>
  <link>https://aikkala.github.io/residual-thoughts/posts/llm-calibration/</link>
  <description><![CDATA[ 





<p>Large language models can be prompted to explicitly express <em>confidence</em> in their answers. However, the expressed confidence may not correlate with correctness of the answers: Ask a 3B model who directed an obscure 1970s film and it might answer with the same assured tone as when you ask it the capital of France. This is miscalibration – a mismatch between expressed and warranted confidence – and it’s a fundamental problem for any system that needs to communicate its own reliability.</p>
<p>The interesting question isn’t just <em>can we fix this</em> but <em>why does it happen in the first place?</em> Two very different accounts are possible:</p>
<ol type="1">
<li><strong>Representation failure</strong>: the model simply doesn’t know what it doesn’t know – the uncertainty information isn’t in the model at all, and training on confidence labels can’t create it from scratch.</li>
<li><strong>Expression failure</strong>: the model <em>does</em> have the relevant internal signal, but lacks a mechanism to translate it into a confidence output.</li>
</ol>
<p>These have different implications. If it’s a representation problem, there’s not much we could apart from training a bigger model with more capacity to store factual answers. If it’s an expression problem, targeted training may be enough to <em>calibrate</em> the model translate uncertainty into the expressed confidence value. This post describes an experiment that tried to find out which one it is.</p>
<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The Setup</h2>
<p>We finetuned Llama3.2-3B-Instruct using <a href="https://arxiv.org/abs/2402.03300">GRPO</a> with a reward structure that directly incentivises calibrated confidence. For each question, the model had to produce a JSON array:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode json code-with-copy"><code class="sourceCode json"><span id="cb1-1"><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"high"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">,</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Paris"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span></code></pre></div></div>
<p>where the first element is its confidence (<code>"high"</code> or <code>"low"</code>), and the second is its answer. We then rewarded or penalised based on whether it was right and how confident it claimed to be:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th></th>
<th>High confidence</th>
<th>Low confidence</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>Correct</strong></td>
<td>+2</td>
<td>+1</td>
</tr>
<tr class="even">
<td><strong>Incorrect</strong></td>
<td>−2</td>
<td>−1</td>
</tr>
<tr class="odd">
<td><strong>Unparseable</strong></td>
<td>−1.5</td>
<td>—</td>
</tr>
</tbody>
</table>
<p>High confidence is only the rational choice when the model expects to be right more than 50% of the time. An unparseable output gets −1.5 – worse than a low-confidence wrong answer – so the model won’t learn to output arbitrary outputs.</p>
<p>We trained on <a href="https://arxiv.org/abs/2212.10511">PopQA</a>, a factual QA dataset built from Wikidata with entity popularity scores that serve as a natural difficulty proxy. One important practical finding: training data difficulty acts as an implicit regulariser. Training on questions the model almost always gets right causes it to collapse to always saying “high confidence”; training on questions it almost never gets right causes it to always say “low confidence”. We found the sweet spot at <code>min_popularity=20,000</code>, where training accuracy sits around 30–40% and both confidence labels are sometimes optimal. Training at <code>min_popularity=50,000</code> (easier questions) produced a finetuned model with ECE 0.49 – nearly identical to the untrained base model.</p>
<p>We also tested two baselines using the base model without finetuning: <strong>direct prompting</strong> (same JSON format, no reasoning) and <strong>chain-of-thought prompting</strong> (brief reasoning before the answer).</p>
</section>
<section id="behavioral-results" class="level2">
<h2 class="anchored" data-anchor-id="behavioral-results">Behavioral Results</h2>
<p>Averaged across three random seeds:</p>
<p><strong>PopQA (in-distribution)</strong></p>
<table class="caption-top table">
<colgroup>
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Metric</th>
<th>Base direct</th>
<th>Base CoT</th>
<th>Finetuned</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>ECE ↓</td>
<td>0.525 ± 0.014</td>
<td>0.324 ± 0.023</td>
<td><strong>0.069 ± 0.007</strong></td>
</tr>
<tr class="even">
<td>Accuracy ↑</td>
<td>0.165 ± 0.003</td>
<td><strong>0.201 ± 0.002</strong></td>
<td><strong>0.201 ± 0.005</strong></td>
</tr>
<tr class="odd">
<td>High-conf accuracy ↑</td>
<td>0.194 ± 0.007</td>
<td>0.294 ± 0.010</td>
<td><strong>0.603 ± 0.013</strong></td>
</tr>
<tr class="even">
<td>High-conf rate</td>
<td>0.735 ± 0.021</td>
<td>0.531 ± 0.028</td>
<td>0.202 ± 0.007</td>
</tr>
<tr class="odd">
<td>Calibration gap ↑</td>
<td>0.107 ± 0.031</td>
<td>0.196 ± 0.006</td>
<td><strong>0.503 ± 0.023</strong></td>
</tr>
</tbody>
</table>
<p>ECE (expected calibration error) drops 87% from base to finetuned. But the more interpretable number is what happens to <em>high-confidence predictions</em>: the base model says “high confidence” on 73.5% of questions and is right on 19.4% of them. The finetuned model says “high confidence” on just 20.2% of questions — and is right on 60.3% of them. It learned to be selective.</p>
<p>The calibration gap (accuracy on high-conf predictions minus accuracy on low-conf predictions) grows nearly five-fold: 0.107 -&gt; 0.503. This implies the model learns that its confidence label actually means something.</p>
<p>The popularity quartile breakdown is particularly clean — the finetuned model’s high-confidence rate tracks its actual accuracy across difficulty levels:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Quartile</th>
<th>Accuracy</th>
<th>High-conf rate</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Q1 (hardest)</td>
<td>12.9% ± 0.5%</td>
<td>8.6% ± 0.3%</td>
</tr>
<tr class="even">
<td>Q2</td>
<td>8.8% ± 1.8%</td>
<td>12.2% ± 1.3%</td>
</tr>
<tr class="odd">
<td>Q3</td>
<td>15.5% ± 2.4%</td>
<td>15.2% ± 1.3%</td>
</tr>
<tr class="even">
<td>Q4 (easiest)</td>
<td>43.2% ± 2.3%</td>
<td>44.5% ± 3.1%</td>
</tr>
</tbody>
</table>
<p>The base model maintains 58–78% high-confidence rates regardless of difficulty.</p>
<p><strong>Does it generalise?</strong></p>
<p>We tested on TriviaQA without any further training:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Metric</th>
<th>Base direct</th>
<th>Base CoT</th>
<th>Finetuned</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>ECE ↓</td>
<td>0.322 ± 0.003</td>
<td><strong>0.293 ± 0.024</strong></td>
<td><strong>0.290 ± 0.010</strong></td>
</tr>
<tr class="even">
<td>Accuracy ↑</td>
<td>0.511 ± 0.005</td>
<td><strong>0.538 ± 0.019</strong></td>
<td><strong>0.532 ± 0.020</strong></td>
</tr>
<tr class="odd">
<td>High-conf accuracy ↑</td>
<td>0.550 ± 0.004</td>
<td><strong>0.591 ± 0.027</strong></td>
<td><strong>0.602 ± 0.018</strong></td>
</tr>
<tr class="even">
<td>High-conf rate</td>
<td>0.892 ± 0.012</td>
<td>0.798 ± 0.004</td>
<td>0.700 ± 0.022</td>
</tr>
<tr class="odd">
<td>Calibration gap ↑</td>
<td><strong>0.366 ± 0.043</strong></td>
<td>0.262 ± 0.042</td>
<td>0.232 ± 0.026</td>
</tr>
</tbody>
</table>
<p>The ECE improvement is modest (0.322 -&gt; 0.290) compared to PopQA, and calibration gap decreases (0.366 -&gt; 0.232). Furthermore, CoT achieves comparable calibration to the finetuned model (ECE 0.293 vs 0.290). This indicatest that the finetuned model’s calibration behavior does not generalise beyond its training distribution.</p>
<p><strong>Accuracy as a side effect</strong></p>
<p>A somewhat surprising finding: calibration training also improved factual accuracy on PopQA by +3.5% ± 0.8%, statistically significant in all three seeds (McNemar’s test, p &lt; 0.005). The effect is concentrated in higher-popularity questions (+7.1% in Q4 vs +1.9% in Q1) and is absent on TriviaQA. This is consistent with RL training improving factual recall as a side effect – a pattern also observed in <a href="https://arxiv.org/abs/2402.03300">DeepSeek-R1</a> for reasoning tasks. On examples where the finetuned model expressed high confidence, the base model was already correct 55–58% of the time (vs 16.5% overall), confirming the model learned to select questions it was already close to answering correctly.</p>
</section>
<section id="whats-happening-inside" class="level2">
<h2 class="anchored" data-anchor-id="whats-happening-inside">What’s Happening Inside?</h2>
<p>Here’s where it gets interesting. We trained linear probes on the residual stream activations at the position of the final prompt token – the point right before the model starts generating – for all 28 layers of both the base and finetuned models. This approach is similar to that presented in <a href="https://arxiv.org/abs/2304.13734">Azaria &amp; Mitchell, 2023</a>, where they trained classifiers to predict the correctness of a statement based on hidden layer activations.</p>
<p>The question: does the internal representation predict whether the model will get the answer right, and does this change after finetuning?</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://aikkala.github.io/residual-thoughts/posts/llm-calibration/probe_results.png" class="img-fluid figure-img"></p>
<figcaption>Probe AUC across transformer layers for base model, finetuned model, and cross-dataset transfer (train on PopQA, test on TriviaQA).</figcaption>
</figure>
</div>
<p><strong>The base model already knows.</strong> Probe AUC rises from 0.70 at layer 0 to a broad plateau of ~0.85–0.86 from layer 9 onwards. Before any calibration training, the model’s internal representations are already strongly predictive of whether it will answer correctly. The uncertainty information is <em>there</em> – the model just doesn’t express it.</p>
<p><strong>Finetuning reduces linear probe AUC.</strong> The finetuned model shows consistently lower probe AUC, especially across later layers, a gap of about 0.05 relative to the base model. This is the surprising result: dramatically better behavioral calibration (ECE 0.069 vs 0.525) is accompanied by <em>less</em> linearly separable internal representations. The correctness-predictive information becomes harder to decode with a linear probe, yet the model’s outputs become much better calibrated.</p>
<p>This suggests calibration training reorganises how uncertainty information is encoded and used, rather than simply amplifying an existing signal. The finetuning changes the representational geometry in a way that’s less decodable by a logistic probe but more functionally coupled to the confidence output. Why? A few possibilities:</p>
<ul>
<li>The uncertainty signal gets encoded in a more nonlinear or distributed form after finetuning, harder for a linear probe to capture but accessible to the model’s own attention and feedforward mechanisms.</li>
<li>GRPO training partially “uses up” the correctness-predictive signal by routing it through the confidence output, leaving a weaker residual in the activations.</li>
<li>The probe captures both genuine uncertainty information and question-difficulty surface features; finetuning on filtered training data (<code>min_popularity ≥ 20,000</code>) selectively reduces the surface-feature component.</li>
</ul>
<p>Distinguishing between these would require causal intervention experiments – activation patching to test whether specific layer activations causally influence the confidence output. We leave that for future work.</p>
<p><strong>Does the representation generalise across datasets?</strong></p>
<p>We trained probes on PopQA activations and evaluated on TriviaQA activations (with the test set downsampled to match the 15% PopQA positive rate). Cross-dataset AUC peaks (&gt;0.6) in mid-layers (12–19), with near-chance performance in the first 7 layers. The early-layer signal doesn’t transfer — it’s capturing PopQA-specific question features. The mid-layer signal generalises, consistent with those layers encoding more abstract semantic content.</p>
</section>
<section id="what-this-means" class="level2">
<h2 class="anchored" data-anchor-id="what-this-means">What This Means</h2>
<p>The central finding is that miscalibration in this 3B model is, to a large extent, a <strong>failure of expression, not representation</strong>. The model knows what it doesn’t know – the information is in the residual stream – but without targeted training it can’t translate that signal into its outputs.</p>
<p>GRPO with calibration rewards installs this translation mechanism. But it does so by reorganising internal representations rather than amplifying them, which raises new questions about the nature of learned confidence.</p>
<p>A few practical takeaways:</p>
<ul>
<li><strong>Training data difficulty is a critical hyperparameter</strong> for calibration training. Questions that are too easy lead to confidence values that are always high. Questions that are too hard lead to confidence values that are always low. The reward function alone doesn’t determine the dynamics; the difficulty distribution of training data interacts with it.</li>
<li><strong>CoT prompting is a reasonable calibration intervention</strong> when models have meaningful domain knowledge (~40–60% accuracy). For harder tasks at the edges of model knowledge, finetuning is needed.</li>
<li><strong>The base model’s residual stream already contains correctness-predictive information</strong> that could in principle be extracted with a linear probe, without any finetuning. This enables deployment-time confidence estimation that doesn’t require any model modification.</li>
</ul>
</section>
<section id="limitations-and-future-work" class="level2">
<h2 class="anchored" data-anchor-id="limitations-and-future-work">Limitations and Future Work</h2>
<ul>
<li>All experiments use a single 3B model, so scale generalisation is unknown.</li>
<li>The binary confidence format is a significant simplification – three or more bins would require non-trivial reward design to prevent the model from using intermediate bins as a hedge.</li>
<li>The probe AUC decrease interpretation is correlational; causal intervention experiments would provide stronger evidence.</li>
</ul>
<p>The most interesting open question is the mechanism behind the probe AUC decrease. Activation patching experiments could establish whether specific layers causally drive the confidence output, and nonlinear probes could test whether the uncertainty information is present but nonlinearly encoded after finetuning. If either finds a clean answer, it would meaningfully advance our understanding of how calibration training works mechanistically.</p>
<hr>
<p><em>Code is available at <a href="https://github.com/aikkala/llm-calibration">github.com/aikkala/llm-calibration</a>. Questions and feedback welcome.</em></p>



</section>

 ]]></description>
  <category>calibration</category>
  <category>reinforcement-learning</category>
  <category>mechanistic-interpretability</category>
  <guid>https://aikkala.github.io/residual-thoughts/posts/llm-calibration/</guid>
  <pubDate>Sun, 24 May 2026 21:00:00 GMT</pubDate>
  <media:content url="https://aikkala.github.io/residual-thoughts/posts/llm-calibration/probe_results.png" medium="image" type="image/png" height="64" width="144"/>
</item>
</channel>
</rss>
